# 出題インデックス（既出テーマ一覧）

新規問題を作るときは、**このファイルだけ**を確認してテーマ・観点の重複を避けること。
各問題の詳細は該当フォルダの `README.md` を参照（普段は開かなくてよい）。

> 運用ルール：新しい問題を出したら、**この表に1行追記**する（番号・テーマ・観点カテゴリ・主なNGポイント）。
> 観点カテゴリは `セキュリティ / 設計 / パフォーマンス / テスト容易性 / 例外処理 / 並行処理 / API設計 / ログ設計` から主要な1つを選ぶ。

## 観点カテゴリ 消化状況（偏り防止の目安）

| 観点カテゴリ | 出題数 | 該当問 |
|---|---|---|
| セキュリティ | 2 | 01, 15 |
| パフォーマンス（N+1等） | 2 | 02, 13 |
| トランザクション/設計 | 1 | 03 |
| バリデーション/DTO設計 | 1 | 04 |
| 例外処理 | 1 | 05 |
| 並行処理・スレッド安全性 | 2 | 06, 10 |
| ログ設計・機密情報 | 2 | 07, 11 |
| テスト容易性・DI | 2 | 08, 12 |
| REST API設計 | 1 | 09 |
| 設定値のハードコーディング | 1 | 14 |
| 設計・型安全性（enum/OOP） | 1 | 16 |
| カプセル化・不変性（防御的コピー） | 1 | 17 |
| オブジェクトの同一性（equals/hashCode契約） | 1 | 18 |
| 金銭計算の型・正確性（BigDecimal/丸め） | 1 | 19 |
| ファイルI/O・リソース管理（バッチ処理） | 1 | 20 |
| 外部API連携の信頼性（タイムアウト/リトライ） | 1 | 21 |
| 非同期処理とトランザクション境界（Springプロキシ） | 1 | 22 |
| キャッシュ設計（不整合/スレッド安全性/スタンピード） | 1 | 23 |
| 日付・時刻/タイムゾーン（Date/SimpleDateFormat） | 1 | 24 |
| 認可・アクセス制御（IDOR/権限昇格） | 1 | 25 |
| 冪等性・重複処理防止（Webhook/リトライ） | 1 | 26 |
| DB楽観/悲観ロック（lost update・二重予約） | 1 | 27 |
| ファイルアップロードのセキュリティ（パストラバーサル/無制限アップロード） | 1 | 28 |

## 既出テーマ一覧

| No | テーマ | 観点カテゴリ | 主なNGポイント |
|----|--------|------------|--------------|
| 01 | ユーザー登録APIの設計とセキュリティ | セキュリティ | パスワード平文保存 / Fat Controller |
| 02 | ユーザー一覧と投稿件数取得 | パフォーマンス | N+1問題（ループ内でgetPosts） |
| 03 | 注文処理（在庫・注文・ポイント一括更新） | トランザクション/設計 | @Transactional欠如で部分コミット / 例外握りつぶし / 存在チェックなし / 同時実行制御なし |
| 04 | ユーザープロフィール更新API | バリデーション/DTO設計 | マスアサインメント脆弱性 / Entity直返しで情報漏洩 / DTO不在 / バリデーション欠如 |
| 05 | 商品取得・購入APIのエラー処理 | 例外処理 | Optional.get()乱用 / 例外握りつぶしreturn null / HTTPステータス不統一 |
| 06 | 請求書番号の発行サービス | 並行処理・スレッド安全性 | 共有フィールドのレースコンディション（read-modify-write） |
| 07 | ログイン処理のログ出力 | ログ設計・機密情報 | 平文パスワードをログ出力 / メール(PII)を平文ログ |
| 08 | 支払い金額を計算するサービス | テスト容易性・DI | 時刻等をnew直書き/static依存でテスト困難 |
| 09 | 注文APIのコントローラ | REST API設計 | @RequestMappingでGET削除可 / 常に200 / 動詞URL |
| 10 | 抽選キャンペーンの応募受付 | 並行処理・スレッド安全性 | Check-Then-Act競合（containsKey→putのすきま） |
| 11 | 外部決済ゲートウェイ呼び出し | ログ設計・機密情報 | カード番号/CVV/トークンをログ垂れ流し（PCI-DSS違反）/ 例外の扱い |
| 12 | パスワード再設定の申請サービス | テスト容易性・DI | MailSenderをnew直書きでテスト不能 / 時刻・トークンの非決定性 |
| 13 | ブログ記事一覧を著者名つきで返す | パフォーマンス | N+1問題（ループ内findById）/ 全件取得（ページングなし） |
| 14 | 日次売上レポートを出力するサービス | 設定値のハードコーディング | パス/メール/税率/しきい値を直書き（外部化なし）/ now()直書きでテスト不能 / 例外握りつぶし / リソースリーク |
| 15 | 商品検索サービス | セキュリティ | SQLインジェクション（ユーザー入力を文字列連結）/ リソースリーク / 例外握りつぶし |
| 16 | 注文ステータスの管理 | 設計・型安全性（enum/OOP） | ステータスをマジックストリングで管理 / 定数化されず表記ゆれ("PAYED"/"PAID")・==比較のバグ / enum不在で網羅性・状態遷移を保証できない |
| 17 | 買い物カゴの中身を保持するクラス | カプセル化・不変性（防御的コピー） | コンストラクタ/getterでミュータブルなListの参照を漏らす（防御的コピー不在）/ 可変なDate保持 / 全setter公開で不変にできない |
| 18 | 発行済みクーポンの二重発行チェック | オブジェクトの同一性（equals/hashCode契約） | equals/hashCode未実装でSet.containsが常にfalse→二重発行を防げない / valueオブジェクトが可変（setter公開） |
| 19 | 口座間の送金サービス | 金銭計算の型・正確性（BigDecimal/丸め） | 金額をdoubleで計算し誤差（BigDecimal不在）/ @Transactional欠如で出金だけ反映（部分コミット）/ 残高read-modify-writeのlost update（ロックなし）/ 金額・残高不足・自己送金のバリデーション欠如 / Optional.get()で存在チェックなし / 例外握りつぶし+boolean返しで原因喪失 |
| 20 | 商品マスタCSVの一括取り込み | ファイルI/O・リソース管理（バッチ処理） | try-with-resources不使用でBufferedReaderがリーク（例外時も閉じない）/ 全行をListに読み込みメモリ圧迫（ストリーミングでない）/ @Transactional欠如＋ループ内即saveで部分取込・再実行不能 / 1件ずつsaveでバッチ化なし（性能）/ 例外握りつぶし+文字列返しで失敗行・原因が不明 / cols長さ・数値・負数の検証なし＆ヘッダ行スキップなし / split(",")の素朴処理でカンマ入りの値が壊れる / FileReaderが環境依存の文字コードで文字化け |
| 21 | 外部倉庫システム(WMS)への在庫問い合わせ | 外部API連携の信頼性（タイムアウト/リトライ） | RestTemplateをnew直書き＆タイムアウト未設定→WMS遅延でスレッド枯渇（信頼性）＋DIされずテスト不能 / ループ内で1件ずつ同期呼び出し（バッチ・並列化なし＝擬似N+1で性能劣化）/ 例外握りつぶしcontinueで失敗商品が黙って消える（在庫ありと誤認しうる）/ リトライ・フォールバック・タイムアウト境界なし / URLを文字列連結でクエリ組立（URLエンコードなし→特殊文字で破損・インジェクション）/ responseのnullチェックなしでNPE / URLハードコードで環境切替不能 |
| 22 | 会員登録とその後続処理（クーポン付与・メール・集計） | 非同期処理とトランザクション境界（Springプロキシ） | 同一クラス内の自己呼び出しで @Async / @Transactional が無効化（プロキシを経由しない）/ register に @Transactional がなく会員保存とクーポン付与が別トランザクション＝部分コミット / 仮に@Asyncが効くとコミット前レコードを別スレッドが読むレース（NoSuchElement）/ 取り消せない副作用（メール送信）をコミット確定前に実行 / new Thread().start() の無制限生成でスレッド枯渇・例外消失（プール/ExecutorService不在）/ @Async void で例外がどこにも伝わらない / e.printStackTrace() の握りつぶし＋ロガー不使用 / Optional.get() で存在チェックなし / ステータス"ACTIVE"・クーポンコード・金額1000のマジック値（enum・設定外部化なし）/ メール重複・形式のバリデーション欠如 |
| 23 | 商品価格キャッシュサービス | キャッシュ設計（不整合/スレッド安全性/スタンピード） | updatePrice時にキャッシュを無効化せず古い価格を返し続ける（キャッシュ不整合＝核心）/ 手製HashMapを複数スレッドで読み書き（非スレッドセーフ→破損/無限ループ）/ containsKey→計算→putのcheck-then-actでスタンピード（同一商品を複数スレッドが同時にDB計算）/ TTL・上限なしで永久保持＝メモリリーク＆いつまでも古い / 可変なProductPriceをキャッシュから直接返し呼び出し側の変更でキャッシュ汚染（防御的コピー欠如）/ 割引をdoubleで計算し金額誤差（BigDecimal不在）/ 割引率0.1のマジックナンバー（外部化なし）/ Optional.get()で存在チェックなし / warmUpの例外握りつぶし（printStackTrace・ロガー不使用） |
| 24 | サブスクリプションの月次請求バッチ | 日付・時刻/タイムゾーン（Date/SimpleDateFormat） | staticなSimpleDateFormatをバッチ・Web両スレッドで共有（非スレッドセーフ→日付が壊れる/例外）/ 「30日後」を次回請求日にして月をまたぐたび請求日がズレる（月加算でない）/ 時刻成分つきDateで請求日を保持し比較がバッチ実行時刻に依存（当日請求されない・翌日二重）/ タイムゾーン未指定でサーバーTZ依存（UTC運用だと日本の顧客の日付が1日ずれる）/ 無料期間7日をミリ秒割り算で判定（startDateの時刻成分で丸1日ぶれる）/ @Transactional欠如＋冪等性なしで再実行・途中失敗時に二重請求 / 取り消せないメール送信をDB確定前に実行 / 例外握りつぶしcontinueで失敗契約が黙って飛ばされる（ロガー不使用）/ reserveCancelはparse失敗を握りつぶして成功扱いのままsave / Optional.get()で存在チェックなし / findAll全件ロード＋1件ずつsave（スケールしない）/ 7日・30日のマジックナンバー |
| 25 | 経費精算の承認API | 認可・アクセス制御（IDOR/権限昇格） | 承認者IDをリクエストパラメータで受け取り信頼＝なりすまし放題（認証情報から取るべき）/ 「申請者の上長か」の認可チェックなしで他人の申請を承認できる＋自己承認も可能（IDOR）/ GET /{id}・一覧が全社員の申請を無条件返却（水平権限昇格）＋Entity直返しで振込先口座番号まで漏洩 / ステータス遷移チェックなしで承認済みを再承認→Payment二重作成＝二重振込 / 役職を文字列"部長"で比較（enum・ロール設計なし、表記ゆれで権限判定が壊れる）/ 10万円のマジックナンバー / @Transactional欠如でExpense更新とPayment作成が分離＝部分コミット / 例外握りつぶし＋常に200 OKで文字列返し（エラーがクライアントに伝わらない）/ ログに申請者名・口座番号（PII/機密）を文字列連結で出力・監査ログは無し |
| 26 | 決済Webhookの受信処理 | 冪等性・重複処理防止（Webhook/リトライ） | 署名(X-Signature)を受け取るだけで検証せず＝偽Webhookで任意の注文を支払い済みにできる（なりすまし）/ 冪等性なしで再送のたびに二重処理＝ポイント二重付与・確認メール二重送信・状態の再上書き（決済プロバイダは2xxまで再送する前提を見落とし）/ 入金額をペイロードから信頼し自社の注文金額と照合しない（過少入金でも支払い済みになる）/ 重い処理（DB更新＋メール送信）を受信スレッドで同期実行し応答が遅い→プロバイダのタイムアウトで再送を誘発 / 例外を握りつぶし常に"OK"(200)を返す＝本当の失敗も再送されず入金取りこぼし＆原因喪失 / @Transactional欠如で注文更新とポイント付与が別トランザクション＝部分コミット / 取り消せないメール送信を確定前・処理途中で実行 / Optional.get()で注文存在チェックなし / "PAID"・"payment.succeeded"のマジックストリング（enum不在）/ Map<String,Object>の無検査キャストとキー欠落でNPE（型安全性なし）/ ログにメール(PII)・署名(機密)を文字列連結で出力 |
| 27 | イベントの座席予約サービス | DB楽観/悲観ロック（lost update・二重予約） | 空席をread→statusをRESERVEDにwriteするread-modify-writeを同時実行制御なしで行い、複数ユーザーが同じ空席を掴んで二重予約＝lost update（@Versionによる楽観ロックもSELECT FOR UPDATEの悲観ロックもない＝核心）/ Seatエンティティに@Versionフィールドがなく楽観ロックが効かない / @Transactional欠如で座席更新と予約レコード作成が別トランザクション＝部分コミット（座席はRESERVEDなのに予約レコードなし等）/ reserveは空席が無いとtargetがnullのままNPE（存在チェックなし）、reserveSeatはOptional.get()で存在チェックなし / reserveSeatは既にRESERVEDの座席でも状態チェックせず上書き予約できる（他人の予約を奪う）/ ステータスを"AVAILABLE"/"RESERVED"のマジックストリングで管理（enum不在・表記ゆれと網羅性の欠如）/ findByEventIdで全座席をロードしアプリ側forループで空席探索（DBのWHERE/LIMITを使わずスケールしない）/ 取り消せない確認メール送信をトランザクション確定前に実行 / 例外握りつぶし+printStackTrace+文字列返しで原因喪失・失敗がクライアントに伝わらない / eventId/userId/seatIdのバリデーション欠如 |
| 28 | プロフィール画像アップロードサービス | ファイルアップロードのセキュリティ（パストラバーサル/無制限アップロード） | ユーザー入力のファイル名(getOriginalFilename)をそのまま保存パスに文字列連結＝パストラバーサル（"../../etc/..."で任意の場所に書き込み・上書き）/ 拡張子・MIMEタイプ・マジックバイト検証なしで任意ファイル(.jsp/.sh等)をアップロード可＝リモートコード実行の温床 / ファイルサイズ上限なしで巨大ファイルによるディスク枯渇・DoS / InputStream/FileOutputStreamがtry-with-resources不使用で例外時にリーク＋outのみclose・inは閉じない / MultipartFileのnull/empty(isEmpty)チェックなしでNPE・0バイト保存 / 同名ファイルの衝突チェックなしで他ユーザーのアバターを上書き / 保存パスをハードコード＆OS依存の区切りで環境非依存でない / ファイル保存(I/O)とDB更新が別で@Transactional・整合性なし（ファイルは保存されたがDB更新失敗で孤児ファイル、逆も）/ サーバー内部の絶対パスをレスポンスで返し情報漏洩 / Optional.get()でユーザー存在チェックなし / 例外握りつぶし+printStackTraceでreturn nullし原因喪失・失敗がクライアントに伝わらない |

index_theme=会員通知サービスの依存の受け取り方（フィールド注入→コンストラクタ注入）
perspective=設計・DI・テスト容易性
ng=フィールドインジェクションで依存が隠れコンストラクタから必要物が読めない / finalにできず不変にできない / new不能でSpring無しの単体テストが困難(モックにリフレクション必要) / 依存増加に気付けずクラス肥大化しても警告が出ない / 循環参照が実行時まで隠れる / Optional.get()で存在チェックなし

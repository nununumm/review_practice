# 【ストック】支払金額の一致判定と値引き計算

**📅 出題日**: （ストック作成）

## 📋 レビュー課題

支払処理のサービスです。顧客が支払った金額（`paid`）が請求金額（`billed`）とぴったり一致するかを判定し、
さらに請求金額から割引率をかけた「割引後の支払金額」を計算して返します。
金額は誤差を避けるため `double` ではなく `BigDecimal` で扱っています。

対象: `bad/PaymentAmountService.java`
「どこが問題か、どう直すべきか」をレビューしてみてください。

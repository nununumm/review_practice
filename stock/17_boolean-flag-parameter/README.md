# 【ストック】通知送信サービスのフラグ引数

**📅 出題日**: （ストック作成）

## 📋 レビュー課題

社内システムの通知送信サービスです。ユーザーへの通知を「メール / SMS / プッシュ通知」のどれで送るか、
「緊急かどうか」「静かに送るか（通知音なし）」「既読管理をするか」などを、メソッドの引数で切り替えて
1つのメソッドにまとめて送っています。呼び出し側は真偽値（true / false）を並べて指定します。

対象: `bad/NotificationService.java`
「どこが問題か、どう直すべきか」をレビューしてみてください。

# 📥 問題ストック運用ボード（未着手 / 着手中 / 完了）

クレジット期間中に問題と答えを**先にまとめて作りだめ**しておき、あとで1問ずつ取り出して解く運用の一覧表です。
（テーマ・観点・番号の正式カタログは [INDEX.md](INDEX.md)。ここは「今どれをやっているか」の進捗ボード。）

## 🔢 番号のルール（重要）

ストックは **並び番号（`01〜`）だけ** を持ち、**本番の問題番号は着手した瞬間に採番**します
（＝そのとき既存の最大番号 +1）。こうすることで、好きな順に取り出しても番号が常に連番になり、
「解いた問題に似たもう1問を後から差し込む」ことをしても歯抜けになりません。

- 例）今の最大が第28問 → 並び番号どれを着手しても、最初の着手が第29問、次が第30問…と順に振られる。
- 採番・INDEXへの追記は `start-problem.ps1` が自動で行います。

## 🔁 運用フロー（Claude不要で回せる）

```
stock/NN_topic/            ← 未着手プール（NN は並び番号。答えは .answer/ に封印済み）
   ├── README.md           ← 問題文だけ（見出しは「【ストック】…」）
   ├── bad/Xxx.java        ← レビュー対象のNGコード
   └── .answer/            ← ★開かないこと（ネタバレ）
        ├── good/          ← 模範解答コード
        ├── ANSWER.md      ← NGポイント / 改善案 / レビュー例文
        └── meta.txt       ← 採番時にINDEXへ書く情報（スクリプト用）

① 着手する:  powershell -File scripts\start-problem.ps1  -Num 1
             → 本番番号を採番（最大+1）し、stock/01_* を RR_* としてルートへ移動。
             → README見出しが「第R問：…」になり、INDEXにも1行追加される。
             → bad/ と README を見て、自分でレビューを書く。
② 答え解禁:  powershell -File scripts\reveal-answer.ps1 -Num R   （R=着手時に振られた本番番号）
             → .answer/good を good/ に出し、ANSWER.md を README 末尾へ追記。INDEXのNG欄も埋まる。
③ 片付け(任意): powershell -File scripts\archive-done.ps1 -Num R
```

> ⚠️ **`.answer/` は自分では開かないこと。** ここを開くと「まず自分で考える」練習が台無しになります。
> 答えを見たくなったら②の解禁スクリプトを実行してください（実行にClaudeは不要です）。

## 📊 進捗（並び番号は着手順ではなく“在庫の並び”。本番番号は着手時に確定）

| 並び | テーマ | 主観点 | 難易度 | 状態 | 本番番号 |
|----|--------|--------|--------|------|--------|
| 01 | 注文明細の集計処理（Stream/コレクション） | 設計・可読性 | 低〜中 | 未着手 | - |
| 02 | 会員情報照会サービス（null安全・Optional） | API設計・堅牢性 | 低 | 未着手 | - |
| 03 | 商品・注文APIのエラー処理（@ControllerAdvice不在） | 例外処理・API設計 | 中 | 未着手 | - |
| 04 | 帳票生成サービス（シングルトンBeanの可変状態） | 並行処理・DI | 中〜高 | 未着手 | - |
| 05 | 商品一覧API（ページング・入力境界） | パフォーマンス・堅牢性 | 中 | 未着手 | - |
| 06 | 記事コメントの投稿・表示（XSS） | セキュリティ | 中 | 未着手 | - |
| 07 | 会員登録リクエストの検証（Bean Validation不在） | バリデーション・堅牢性 | 中 | 未着手 | - |
| 08 | 夜間バッチのログ出力（ロギング設計） | ログ設計 | 低〜中 | 未着手 | - |
| 09 | 商品ランキングの並べ替え（Comparator契約） | 型安全性・設計 | 中 | 未着手 | - |
| 10 | 外部連携処理の例外設計 | 例外処理 | 中 | 未着手 | - |

> 状態・本番番号は手動で更新してください（スクリプトはファイル移動・README/INDEX更新を担当）。
> どれから解くかは難易度・観点を見て自由に選んでOK。似た系統をまとめて解くのも自由です。

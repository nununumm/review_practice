# 復習ログ（Review Log）

コードレビュー特訓の「復習」の記録。新規出題とは別に、過去問を **再レビュー形式**（`bad/` だけを見て記憶で指摘 → 当時の模範解答と答え合わせ）で振り返った履歴を残す。

## 進捗サマリ

全13問すべて模範解答まで完了済み。

| # | テーマ | 中心の観点 | 復習状況 |
|---|--------|-----------|---------|
| 01 | ユーザー登録API | セキュリティ | 未復習 |
| 02 | N+1問題 | パフォーマンス | 未復習 |
| 03 | 注文処理の一括更新 | トランザクション管理 | 未復習 |
| 04 | プロフィール更新API | DTO設計・バリデーション | ✅ 2026-07-19 |
| 05 | エラー処理 | 例外処理 | 未復習 |
| 06 | 請求書番号発行 | 並行処理・スレッド安全性 | 未復習 |
| 07 | ログイン処理のログ | ログ設計・セキュリティ | 未復習 |
| 08 | 支払い金額計算 | テスト容易性・DI | 未復習 |
| 09 | 注文APIコントローラ | REST API設計 | 未復習 |
| 10 | 抽選キャンペーン応募 | 並行処理（Check-Then-Act） | ✅ 2026-07-29 |
| 11 | 決済ゲートウェイ呼び出し | ログ設計・例外処理 | ✅ 2026-07-29 |
| 12 | パスワード再設定申請 | テスト容易性・決定性 | ✅ 2026-08-01 |
| 13 | 記事一覧＋著者名 | パフォーマンス（N+1） | 未復習 |

**次の復習候補**：第13問（N+1・パフォーマンス）。

---

## 復習履歴

### 2026-07-19 ｜ 第4問 プロフィール更新API（DTO設計・バリデーション）

形式：`bad/` のみを見て、記憶で問題点を指摘 → 当時の模範解答と答え合わせ。

**自力で挙げられた点**
- Controller にロジックをベタ書き → サービス層へ切り出す（責務分離）
- Entity をそのままレスポンスに返すと password / role が漏れる → レスポンス用 DTO で絞る（情報漏洩）
- `findById(id).get()` の存在チェック漏れ

**ヒントを経て到達した点**
- **マスアサインメント脆弱性**：`@RequestBody User` で受けると、クライアントが `role` を送り込んで自分を管理者に昇格できる。→ 入力用 DTO（`name` / `email` だけ）で「そもそも受け取らない」構造にする。
  - 当初は「認証を挟んで守る」止まり → 「入り口の型で構造的に受け取らない」に発想を更新。
- **バリデーション**：`@Valid`（Controller 引数）＋ DTO 側の `@NotBlank` / `@Email` はセットで機能する。

**本人が整理できた原則**
- Entity は「DTO の一種」ではなく別物。Entity ＝ 名簿（社外秘）、DTO ＝ 名刺（用途別に渡してよい情報だけ）。
- 「入力用 / 出力用 / Entity」の3つは初学者の標準構成だが固定ではない。原則は
  **「Entity は絶対に外に出さない。窓口には用途に必要な項目だけの箱（DTO）を立てる。形が同じなら使い回し、違うなら分ける」**。

**総評 / 伸びしろ**
- セキュリティ・アーキテクチャの「気づく力」は合格ライン。
- 伸びしろは「守り方の引き出し」。「認証を挟む」より先に「入り口の DTO で構造的に受け取らない」を第一候補に。

---

### 2026-07-29 ｜ 第10問 抽選キャンペーン応募（並行処理・Check-Then-Act）

形式：`bad/CampaignApplicationService.java` を再提示し、記憶で NG ポイントを指摘 → 模範解答と答え合わせ（クイズ形式）。

**自力で挙げられた点**
- `@Autowired` は不要 → テストしづらい（コンストラクタ注入にすべき）
- 合計応募数（`totalApplications`）は DB で管理すべき
- **チェックから保存までに“すきま”がある → 1動作で対応すべき（＝ Check-Then-Act の競合状態）** ← 今回の最重要ポイントを自力で的中
- 応募済みユーザー（`appliedUsers`）もメモリでなく DB 管理すべき

**ヒントを経て到達した点**
- **直し方の型**：事前チェックを捨て、`user_id` に**ユニーク制約**を張って「まず保存 → 被れば DB が弾く（`DataIntegrityViolationException`）→ catch して “応募済み” に翻訳」＝**発想の逆転**（Check-Then-Act → まず実行して DB に判定させる）。
- **“なぜ危ないか” の言語化**：`@Service` は 1 インスタンスを全スレッドで共有するため、
  - `HashMap` への並行書き込みは内部構造を壊す（`HashMap` は並行書き込み非対応）、
  - `totalApplications = totalApplications + 1` は「読む→+1→書く」の 3 ステップで**アトミックでない**ため追い越しでカウントがズレる。
  - 用語：**レースコンディション（競合状態）／アトミック（不可分）**。

**総評 / 伸びしろ**
- Check-Then-Act の核心を自力で突けたのが最大の収穫。合格。
- 伸びしろは「フィックス（直し方）は分かる」の一歩手前、「**なぜ危ないか**」を用語で言語化する力。指摘時の説得力に直結する。

---

### 2026-07-29 ｜ 第11問 決済ゲートウェイ呼び出し（ログ設計・例外処理）

形式：`bad/PaymentGatewayService.java` を再提示し、記憶で NG ポイントを指摘 → 模範解答と答え合わせ（クイズ形式）。観点は【ログ編】【例外編】の 2 本立て。

**自力で挙げられた点**
- 【ログ編】カード番号・API トークンをログに出してはいけない（情報漏洩／PCI-DSS 違反）
- 【例外編】`e.printStackTrace()` と `logger.error(..., e.getMessage())` はダメ → 例外オブジェクト `e` をそのまま渡す（`log.error("...", e)` でスタックトレースごと記録）
- 【ログ編】`response` を丸ごと出さず、中身を選別して安全な項目だけ出す（ヒント後に到達）

**ヒントを経て到達した点**
- 【ログ編・詰め】**CVV は保存・出力そのものが禁止**。カード番号は「下4桁マスク（`****1234`）」で追跡に使えるが、CVV は「マスクすれば OK」ですらなく一切残さない。追跡には秘密でない `orderId` / `amount` を使う。
- 【例外編・本丸】「失敗を種類ごとに区別する」という直感は正解。ただし整理は
  - **業務的な失敗（残高不足で拒否）＝戻り値 `boolean`（`false`）のまま**、
  - **システム故障（通信断・タイムアウト）＝独自例外 `PaymentGatewayException` を投げる**。
  - 作法は「**記録してから包んで投げ直す（wrap and rethrow）**」＝ `log.error("...", e)` の後に `throw new PaymentGatewayException(msg, e)` で原因例外を連鎖。
  - 補足：`NullPointerException` を投げるのはズレ（＝プログラムのバグを表す例外で、ゲートウェイ故障の意味にならない）。

**総評 / 伸びしろ**
- ログ編・例外編とも“方向”は自力で当てられた。
- 伸びしろ：①CVV の特別扱い（保存・出力禁止）を勘所として固定する、②失敗の切り分けを「boolean（業務失敗）／独自例外（システム故障）」というプロの型で言語化する。

---

### 2026-08-01 ｜ 第12問 パスワード再設定申請（テスト容易性・決定性）

形式：`bad/PasswordResetService.java` を再レビュー。指摘 → 解説で答え合わせ。テーマは「このサービスをどう自動テストで守るか（テスト容易性）」。
※このお題は「マジックナンバー廃止」「currentTime の外部注入」という切り口から入ったが、実体は第12問（テスト容易性・決定性）。

**自力で挙げられた点**
- **マジックナンバーを廃止**（`60 * 60 * 1000` ＝有効期限1時間、`"smtp.example.com"` / `587`）
- **`System.currentTimeMillis()` をやめ、時刻を外部注入してフィールド化** ← 今回テーマの核心（非決定性の排除）を自力で的中
- メール送信について「テストのたびに本物のメールが飛んでしまう」危険を自力で診断 ← 最重要ポイントの“なぜダメか”に到達

**ヒント／解説を経て到達した点**
- **処方の修正（半歩のズレを矯正）**：DI すべきは「宛先（`"smtp.example.com"` などの文字列）」ではなく、**「送る部品＝`MailSender` そのもの」**。宛先だけ外出ししても `new SmtpMailSender(...)` が残る限り本物は飛ぶ。→ `MailSender` を**インターフェース**にしてコンストラクタで注入し、テストは**モック**に差し替える。
  - たとえ：直すべきは「宛先住所」ではなく「投函する人（郵便屋さん）ごと練習用に交代させる」こと。
- **すべての NG は同じ病気**：「変わるもの・外に触れるものは自分で `new` せず、外から渡す（＝DI）」で一本化できる。
  - 時刻 → `Clock`（`currentTime` のフィールド化の理想形は `long` ではなく `java.time.Clock`。テストは `Clock.fixed(...)` で時刻を固定でき、有効期限を“ピッタリ”検証できる）
  - トークン → `TokenGenerator`（`UUID.randomUUID()` を差し替え可能に）
  - メール → `MailSender`（インターフェース＋モック）
  - 設定値 → `PasswordResetProperties`（`Duration` で外部化。マジックナンバー廃止）
  - `@Autowired` フィールド注入 → **コンストラクタ注入＋`final`**（必要な部品が一目で分かり、テストは `new` で組み立てられる）

**あわせて学んだ道具（JUnit / Mockito・Java文法）**
- **Mockito**：
  - `mock(型.class)` … その型の“中身が空の偽物”を生成（呼んでも本物は動かないが、呼ばれ方は記録する）。
  - `verify(偽物).メソッド(...)` … 実行の**後**に「そのメソッドが呼ばれたか」を後追い確認（＝過去の確認。呼ばれてなければ失敗）。
  - **引数マッチャー**：`eq("x")`＝ちょうど一致／`any()`＝なんでもよい／`contains("x")`＝部分一致。
  - 罠：1回の呼び出しでマッチャーを1つでも使ったら**全引数をマッチャーに揃える**必要がある（だから生の値も `eq(...)` で包む）。
  - `send` は戻り値なし（`void`）なので `when().thenReturn()` は不要、`verify` だけでよい（戻り値を使うメソッドは `when().thenReturn()` で仕込む）。
- **`型名.class` / `Class`**：`MailSender.class` は「その型そのものを指す名札＝型の履歴書（`Class` オブジェクト）」。`mock()` や `LoggerFactory.getLogger(...)` に「どの型か」を教えるために渡す。実行中に型情報を読んで動く仕組み＝**リフレクション**。

**総評 / 伸びしろ**
- 今回テーマの核心（時刻の外部注入による非決定性の排除）とマジックナンバー廃止を自力で的中。メール送信の危険も自力で診断できたのは大きな収穫。合格。
- 伸びしろ：①「テスト容易性の直し方」を“設定の外出し”で止めず、**“依存オブジェクトごと差し替える（インターフェース＋注入＋モック）”**まで一手で結びつける。②JUnit / Mockito の基本文法（`mock` / `verify` / 引数マッチャー）を手に馴染ませる。
